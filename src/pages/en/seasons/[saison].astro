---
import SaisonView from '@/views/SaisonView.astro';
import { saisons, type SaisonId } from '@/data/saisons';
import { saisonSlugs } from '@/i18n/locales';

export function getStaticPaths() {
  return saisons.map((s) => ({
    params: { saison: saisonSlugs[s.id].en },
    props: {
      saisonId: s.id,
      alternates: { fr: `/saisons/${s.id}/`, de: `/de/jahreszeiten/${saisonSlugs[s.id].de}/` },
    },
  }));
}

interface Props {
  saisonId: SaisonId;
  alternates: Partial<Record<'fr' | 'de', string>>;
}

const { saisonId, alternates } = Astro.props as Props;
---

<SaisonView saisonId={saisonId} locale="en" alternates={alternates} />
