---
import HomeView from '@/views/HomeView.astro';

// FR = langue par défaut, non préfixée (US-061). Traductions : voir src/pages/en/index.astro, src/pages/de/index.astro.
const alternates = { en: '/en/', de: '/de/' };
---

<HomeView locale="fr" alternates={alternates} />
